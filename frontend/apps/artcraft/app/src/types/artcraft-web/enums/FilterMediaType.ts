// File-format filter for media listings. String values match the
// host's database column conventions.
export enum FilterMediaType {
  SCENE_JSON = "scene_json",
  GLB = "glb",
  GLTF = "gltf",
  PMX = "pmx",
  PMD = "pmd",
  VMD = "vmd",
}
